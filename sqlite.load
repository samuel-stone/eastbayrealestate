LOAD DATABASE
     FROM scraper/output/leads.sqlite3
     INTO ${DATABASE_URL}

 WITH include drop, create tables, create indexes, reset sequences

 SET work_mem to '256MB', maintenance_work_mem to '512MB';