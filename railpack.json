{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": [
        "pip install -r requirements.txt",
        "playwright install --with-deps chromium"
      ]
    }
  }
}
